FileReader
        OK 1) est-ce que mettre à dispo good(), bad(), fail(), eof() ?
FASTAFileReader
        OK 1) faire constructeur à partir fichier
        OK 2) utiliser la map
        OK 3) tester la map
        OK 4) supprimer fonctionalité match approximatif -> modifier SpecificReader

SpecificReader
        OK 1) supprimé fonctionalité match approximatif
	2) implémenter des getters const et normal pour _entry_map et les utiliser

ConfigFileReader
	1) tests
	OK 2) implémenter les méthodes const
